[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
    --allure-no-capture
markers =
    smoke: 冒烟测试
    ui: UI 自动化
    login: 登录相关
    positive: 正向用例（需有效账号）
    negative: 负向用例
